# 医生投放评价系统 - 环境变量配置示例
# 复制此文件为 .env 并根据你的环境进行配置

# 服务器配置
RUST_WEB_PORT=8081
RUST_WEB_HOST=0.0.0.0

# 日志配置
RUST_LOG=info
# 生产环境建议: RUST_LOG=warn,rust_web_backend=info

# 数据库配置
DATABASE_URL=sqlite:./data/doctor_analysis.db
# 生产环境可配置为 PostgreSQL 或 MySQL
# DATABASE_URL=postgresql://username:password@localhost/doctor_analysis

# 业务配置
MAX_UPLOAD_SIZE=10485760  # 10MB
SESSION_TIMEOUT=3600      # 1小时
RATE_LIMIT_PER_MINUTE=60  # 每分钟请求限制

# 安全配置
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:8081
JWT_SECRET=your-super-secret-jwt-key-here
API_KEY_HEADER=X-API-Key

# 性能优化
WORKER_THREADS=4
MAX_CONNECTIONS=100
CONNECTION_TIMEOUT=30

# 监控配置
HEALTH_CHECK_ENABLED=true
METRICS_ENABLED=true
PROMETHEUS_PORT=9090
